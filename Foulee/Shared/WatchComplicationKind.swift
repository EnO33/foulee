/// Watch complication kind strings, shared between the watch app and its
/// widget extension so the app can reload a single complication by kind.
enum WatchComplicationKind {
    static let hydration = "com.eno33.foulee.watchHydrationComplication"
}
