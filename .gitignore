# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
xcuserdata/
*.perspectivev3

# Swift Package Manager
.swiftpm/
.build/
Packages/
Package.resolved

# Tuist generated artifacts (re-generated via `tuist generate`)
*.xcodeproj
*.xcworkspace
Derived/
.tuist-bin/
.tuist-cache/
graph.png

# CocoaPods / Carthage (not used, but ignored for safety)
Pods/
Carthage/Build/

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# Secrets
*.env
*.env.local
secrets/

# Signing material decoded by the release workflow (never commit these)
*.p8
*.p12
*.mobileprovision
fastlane/asc_key.p8
fastlane/dist_cert.p12

# Connect IQ developer key (lives in $HOME/.garmin) and build output
*.pem
*.der
*.prg
*.prg.debug.xml
*.iq
FouleeConnectIQ/bin/

# Local screenshots / captures (App Store assets, device + simulator grabs)
IMG_*.PNG
Simulator Screenshot*.png
/appstore-screenshots/

# Local tooling state
.claude/
.cursor/
.aider*
.idea/
.vscode/

# Per-developer signing settings (template is checked in)
Local.xcconfig
